/**
 * DemoFlow's local MCP runtime entry point.
 *
 * Tool implementation is intentionally staged: the first working vertical
 * slice will add project inspection, app lifecycle, and proxy tools here.
 */
const message = "DemoFlow MCP server scaffold. Build the local proxy runtime before enabling tools.";

process.stdout.write(`${message}\n`);
